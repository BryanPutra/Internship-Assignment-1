import * as React from 'react';

interface ICreditCardProps {
}

const CreditCard: React.FunctionComponent<ICreditCardProps> = (props) => {
  return (
    <div></div>
  );
};

export default CreditCard;
