import * as React from 'react';

interface ISettingsProps {
}

const Settings: React.FunctionComponent<ISettingsProps> = (props) => {
  return (
    <div className="flex">bro</div>
  );
};

export default Settings;
